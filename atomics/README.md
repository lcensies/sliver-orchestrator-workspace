# atomics

Atomic Red Team YAML technique library. **Not committed** — populate from the main
`sliver-orchestrator` repo:

```bash
./atomic/fetch.sh ../sliver-orchestrator-workspace/atomics
# refresh / clean:
./atomic/fetch.sh ../sliver-orchestrator-workspace/atomics --clean
```

The Docker lab and `goart` read techniques from this directory.
