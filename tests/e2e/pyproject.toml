[project]
name = "sliver-orchestrator-e2e"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0",
    "httpx>=0.27",
    "pyyaml>=6.0",
    "sseclient-py>=1.8",
]

[tool.pytest.ini_options]
timeout = 30
